[] - Use /caveman skill and the /ubiquitous-language skill when starting a new conversation

## Planning
- Use the /grill-me skill

## Implementation
- Use the /tdd skill
- In typescript/javascript projects, use `bun` instead of node

## Git
- Instead of using git on the command line, use the /graphite skill to access the graphite-cli for managing branches and PRs
